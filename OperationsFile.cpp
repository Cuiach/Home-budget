#include "OperationsFile.h"
/*
void OperationsFile::addOperationToFile(Operation operation)
{
    CMarkup xml;

    if (!xml.Load(DataFile))
    {
        xml.AddElem(DataFile);
        xml.Save(DataFile);
    }

    xml.Load(DataFile);
    xml.FindElem("Incomes")
    xml.IntoElem();

    xml.AddElem("User");
    xml.IntoElem();

    xml.AddElem("ID", AdditionalMethods::convertIntToString(user.getId()));
    xml.AddElem("login", user.getLogin());
    xml.AddElem("password", user.getPassword());
    xml.AddElem("name", user.getName());
    xml.AddElem("lastName", user.getLastName());

    xml.Save(DataFile);
}

void OperationsFile::readOperationsFromFile(int dateFrom, dateTo)
{}
*/
